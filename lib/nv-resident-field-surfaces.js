/**
 * Neighbourhood Voice resident forms: explicit light surfaces so mobile OS / global
 * `color-scheme: dark` does not render comment fields as dark with light text.
 */
export const NV_TEXTAREA_SURFACE = {
  backgroundColor: '#F5F0E6',
  color: '#111827',
  colorScheme: 'light',
}

/** Same palette for single-line fields where dark native styling is an issue (e.g. sign-off). */
export const NV_TEXT_INPUT_SURFACE = {
  backgroundColor: '#F5F0E6',
  color: '#111827',
  colorScheme: 'light',
}
